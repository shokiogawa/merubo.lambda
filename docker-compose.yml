version: '3.7'

services:
  app:
    build:
      context: .
      dockerfile: Docker/Dockerfile.develop
    tty: true
    stdin_open: true
    command: go run main.go
    volumes:
      - ./src:/go/merubo/src
    ports:
      - "8080:80"
    env_file: .env